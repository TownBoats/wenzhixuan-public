重新设计数据流-从读取数据开始
	1.获取到数据-使用解析器解析内容
		对于response和math、code：显示到ui上--使用currResponse、currMath、currCode来存储
		一个标签的内容解析完毕->使用singleTurnResponse、singleTurnMath、singleTurnCode来存储一个带有index的内容
		！还有message！
	2.对于question标签的内容：解析question
		单个question：解析完成后存入singleTurnQuestion（带有index），然后立马进行api调用获取对应answer（可以用简单的contentParser，调用后写入question）。
		
	3.合并message：
		合并singleTurnResponse，singleTurnMath，singleTurnCode以及singleTurnQuestion（考虑带不带question），然后设置message（角色是assistant）。
	
	4.问题卡片：
		写一个问题卡片组件，其中填充内容。点击问题卡片选择回答，将回答和问题都放入用户message。（考虑做问题和用户回答内容的区分）
	
	5.ChatWindow组件：修改显示逻辑。
		当response更新的时候显示response，当response完毕后设置为空，然后设置message，question怎么处理。
		两个方案：
			一、设置true message和用户可见message（感觉不好，没有把ui和数据分开）
			二、调整ui方案，ChatPage的显示逻辑和message分开。
选择第二个方案。
	修改message的数据结构，将question设置为message的子属性。在系统进行一次对话以后，把question设置为真实上下文。
	维护两个message，一个为ui message，一个为true message。
	ui是一定要重构的，更新逻辑也要改。
	
	